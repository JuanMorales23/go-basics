//Se compone de archivos fuente que mejoran la legibilidad del código y pueden incluír funcionalidades adicionales
//Algunos ejemplos son: main (punto de entrada), fmt (formater), math (matemáticas), io (entrada/salida), etc.
//Por convención el nombre de los package van en minúsculas y sin espacios, si es compuesto utilizar _ para separar palabras;

package paquetes

import "fmt"

func Paquetes() {
	fmt.Println("Hola desde el paquete 06-Paquetes")
}
